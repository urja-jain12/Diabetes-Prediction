# Diabetes-Prediction
This project builds a Machine Learning model to predict whether a person is diabetic based on medical attributes such as glucose level, BMI, insulin, and age. Using the Pima Indians Diabetes dataset, the data is preprocessed and scaled before training an SVM classifier. 
